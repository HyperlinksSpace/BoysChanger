{
  "name": "boyschanger",
  "version": "1.0.0",
  "description": "System-wide voice changer for Windows and macOS",
  "main": "dist-electron/main.js",
  "author": "HyperlinksSpace",
  "homepage": "https://github.com/HyperlinksSpace/BoysChanger",
  "repository": {
    "type": "git",
    "url": "https://github.com/HyperlinksSpace/BoysChanger.git"
  },
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.json --noEmit && vite build && electron-builder --publish never",
    "build:dir": "vite build && electron-builder --dir --publish never",
    "preview": "vite preview",
    "electron:dev": "vite",
    "pack:win": "vite build && electron-builder --win --publish never",
    "pack:mac": "vite build && electron-builder --mac --publish never",
    "pack:all": "vite build && electron-builder --win --mac --publish never"
  },
  "build": {
    "appId": "com.boyschanger.app",
    "productName": "BoysChanger",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "icon": "build/icon.png",
    "files": [
      "dist/**/*",
      "dist-electron/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "build/icon.png",
        "to": "icon.png"
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        },
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ],
      "artifactName": "BoysChanger-${version}-Windows-${arch}.${ext}"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": ["x64", "arm64"]
        },
        {
          "target": "zip",
          "arch": ["x64", "arm64"]
        }
      ],
      "category": "public.app-category.music",
      "artifactName": "BoysChanger-${version}-macOS-${arch}.${ext}",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true
    }
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.7.2",
    "vite": "^5.4.11",
    "vite-plugin-electron": "^0.28.8",
    "vite-plugin-electron-renderer": "^0.14.6"
  }
}
